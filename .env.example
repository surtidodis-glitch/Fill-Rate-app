# Cadena de conexión a Postgres (Neon, Supabase, Railway, etc.)
# Formato: postgresql://usuario:password@host:puerto/basededatos?sslmode=require
DATABASE_URL="postgresql://usuario:password@host/basededatos?sslmode=require"
